# 3 分钟作品演示脚本

1. **0:00–0:25 — 问题和架构**：说明这是合成数据的企业制度 RAG；核心目标是让“能回答”变成“只对有权限的人、基于证据地回答”。
2. **0:25–1:10 — ACL**：在 X-Ray 页面以“访客”询问“差旅费用多久内提交报销”，展示空证据与拒答；切换“财务部”，再次提问，展示 FIN-001 与有引用回答。
3. **1:10–1:55 — 可解释检索**：打开证据详情，说明 ACL → BM25 + BGE dense → RRF → rerank；强调 ACL 在召回前执行。
4. **1:55–2:25 — 知识治理**：打开管理页，展示作用域和只读保护；说明生产版将接入上传、审核、切分和索引任务。
5. **2:25–3:00 — 可信交付**：展示 `python eval_pipeline.py --mode test`、Protected Gate、Docker 和 GitHub Actions。说明公开仓只有合成数据，真实数据不进入代码、镜像或日志。
